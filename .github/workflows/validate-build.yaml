name: Validate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            10.x
            8.0.x

      - name: Restore dependencies
        run: dotnet restore CollectionsSpecializedExtensions.slnx

      - name: Build solution
        run: dotnet build CollectionsSpecializedExtensions.slnx --no-restore --configuration Release

      - name: Run tests
        run: dotnet test CollectionsSpecializedExtensions.slnx --no-build --configuration Release --verbosity normal